package constants

// Terminal size constants for the entire application.
// All UI components should use these values to ensure consistent layout.
const (
	// TerminalWidth is the standard terminal width in characters.
	TerminalWidth = 80

	// TerminalHeight is the standard terminal height in lines.
	TerminalHeight = 28
)
